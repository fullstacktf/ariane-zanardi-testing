/**
 * 
 * @param {string} stones A string with each of the stones 
 * 
 * 
 */

export function getNumberOfStonesToRemove(stones) {
    
    const foo = stones.length;
    return foo;
}

export default getNumberOfStonesToRemove;